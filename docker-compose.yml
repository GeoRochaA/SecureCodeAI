services:
  # Backend API
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "3001:3001"
    environment:
      - NODE_ENV=production
      - DATABASE_PATH=/app/data/securecode.db
      - PORT=3001
    volumes:
      - ./backend/data:/app/data
    networks:
      - securecode-network

  # Frontend
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "5173:80"
    networks:
      - securecode-network

networks:
  securecode-network:
    driver: bridge
